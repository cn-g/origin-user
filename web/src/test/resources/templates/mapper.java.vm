package ${package.Mapper}import org.apache.ibatis.annotations.Mapper;

;
${package.Entity}
.${entity};
    ${superMapperClassPackage}
        ;

/**
 * <p>
 * $!{table.comment} Mapper 接口
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
@Mapper
public interface ${table.mapperName} extends ${superMapperClass}<${entity}> {

        }
